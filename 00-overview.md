# 커리어 매칭 및 추천 시스템 최적화 — 프로젝트 개요

> 참고 서비스: [커리어데이](https://www.careerday.jobs)
> 프로젝트 유형: 3인 팀 프로젝트 | 반응형 웹 개발 | 기간: 4일 (MVP)

---

## 1. 프로젝트 소개

### 1.1 주제
**커리어 매칭 및 추천 시스템 최적화**

구직자와 기업 간의 커리어 매칭 품질을 향상시키기 위해 추천 알고리즘을 분석·개선하고, 데이터 기반의 성과 측정 체계를 구축하는 반응형 웹 프로젝트입니다.

### 1.2 목표
- 커리어데이(careerday.jobs)의 실제 매칭 구조를 분석하고 개선 방향을 도출
- 개인 맞춤형 직무·프로젝트 추천 시스템 설계
- A/B 테스트를 통한 매칭 모델 정확도 개선
- 매칭 성공률 및 기업/인재 연결 성과 측정 체계 수립

### 1.3 참고 서비스 선정 근거
커리어데이는 국내 B2B 중심 커리어 매칭 플랫폼으로, 기업과 전문가(프리랜서·인재)를 연결하는 구조입니다. 직무 기반 매칭, 전문가 추천, 프로젝트 매칭 기능을 갖추고 있어 본 프로젝트의 주제와 직접적으로 연관됩니다.

---

## 2. 팀 구성 및 역할

| 팀원 | 담당 과업 | 주요 기여 영역 |
|------|-----------|---------------|
| 팀원 A | 과업 1 데이터 수집 및 분석 + 과업 4 성과 측정 | 데이터 스키마·목 데이터 생성, 분석 대시보드, KPI 카드 |
| 팀원 B | 과업 2 추천 시스템 개선 | 콘텐츠 기반 추천 엔진, JobCard·MatchScore 컴포넌트 |
| 팀원 C | 과업 3 A/B 테스트 및 성능 최적화 | 실험 변환 로직, 이벤트 트래킹, 반응형 성능 최적화 |

> 3인 체제로 팀원 A가 데이터/성과 측정(과업 1+4)을 겸임합니다. 공통 컴포넌트 및 코드 리뷰는 전원 참여합니다.

---

## 3. 전체 일정 (4일 MVP)

> MVP 원칙: 핵심 기능만 동작하면 완성. 완벽함보다 동작하는 데모 우선.

```
       Day 1 (설계)          Day 2 (핵심 구현)       Day 3 (통합)           Day 4 (마무리)
───────────────────────────────────────────────────────────────────────────────────────────
오전   환경설정·레포 구성      추천 엔진 구현          A/B 변환 로직 구현     버그픽스
       데이터 스키마 확정       JobCard·MatchScore      성과 KPI 카드 구현     반응형 최종 검증
       목 데이터 30건 생성      컴포넌트 개발           추천 목록 페이지 통합  Lighthouse 체크
───────────────────────────────────────────────────────────────────────────────────────────
오후   콘텐츠 기반             FilterPanel·            성과 대시보드 기본     발표 자료 준비
       추천 알고리즘 설계       RecommendList 개발      뷰 개발               Vercel 배포
       공통 컴포넌트 기반 구축  API Routes 연결         이벤트 트래킹 연결     최종 발표
───────────────────────────────────────────────────────────────────────────────────────────
담당   팀 전원 (설계 공유)      팀원 B 주도             팀원 A·C 주도          팀 전원
```

### MVP 핵심 기능 범위 (Must Have)

| 기능 | 담당 | Day 목표 |
|------|------|---------|
| 목 데이터 세트 (공고 30건, 사용자 5명) | 팀원 A | Day 1 |
| 콘텐츠 기반 추천 엔진 (스킬 유사도) | 팀원 B | Day 2 |
| JobCard + MatchScore 컴포넌트 | 팀원 B | Day 2 |
| 추천 목록 반응형 페이지 | 팀원 B | Day 3 |
| A/B 변환 로직 + 1개 시나리오 (스코어 표시 방식) | 팀원 C | Day 3 |
| KPI 대시보드 (매칭률·CTR·지원 전환율 카드) | 팀원 A | Day 3 |
| Vercel 배포 + Core Web Vitals ≥ 90 | 팀원 C | Day 4 |

### Nice to Have (시간 여유 시)
- 협업 필터링 추가 (하이브리드 앙상블)
- A/B 시나리오 2번째 추가
- 코호트 리텐션 차트

### 주요 마일스톤
| 마일스톤 | 목표 시점 |
|---------|---------|
| 환경설정 + 목 데이터 완성 | Day 1 오후 |
| 추천 엔진 + 핵심 UI 완성 | Day 2 마감 |
| 전 기능 통합 완성 | Day 3 마감 |
| 배포 + 발표 준비 완료 | Day 4 마감 |

---

## 4. 기술 스택

### 4.1 Frontend
| 분류 | 기술 | 용도 |
|------|------|------|
| 프레임워크 | Next.js 14 (App Router) | SSR/SSG, 라우팅, API Routes |
| 언어 | TypeScript 5.x | 타입 안전성 |
| 스타일 | Tailwind CSS 3.x | 반응형 유틸리티 클래스 |
| 상태관리 | Zustand | 전역 상태 (필터, 사용자 설정) |
| 서버 상태 | React Query (TanStack Query) | API 캐싱, 무한 스크롤 |
| HTTP | Axios | API 요청 |
| 데이터 시각화 | Chart.js + Recharts | 매칭 통계, 성과 대시보드 |

### 4.2 개발 도구
| 분류 | 기술 |
|------|------|
| 패키지 매니저 | pnpm |
| 린트/포맷 | ESLint + Prettier |
| 테스트 | Jest + React Testing Library |
| 배포 | Vercel |
| 협업 | GitHub (PR 기반 코드 리뷰) |

---

## 5. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                         사용자 (브라우저)                      │
└─────────────────────┬───────────────────────────────────────┘
                      │ HTTPS
┌─────────────────────▼───────────────────────────────────────┐
│                  Next.js 14 Frontend (Vercel)                │
│  ┌────────────┐  ┌──────────────┐  ┌─────────────────────┐  │
│  │ Pages /    │  │  Components  │  │   State (Zustand)   │  │
│  │ App Router │  │  JobCard     │  │   - filters         │  │
│  │ - /        │  │  MatchScore  │  │   - userProfile     │  │
│  │ - /jobs    │  │  RecommendList│  │   - abVariant       │  │
│  │ - /profile │  │  ChartWidget │  └─────────────────────┘  │
│  │ - /results │  └──────────────┘                           │
│  └────────────┘                                             │
└─────────────────────┬───────────────────────────────────────┘
                      │ API Routes (Next.js) / REST
┌─────────────────────▼───────────────────────────────────────┐
│                      API Layer                               │
│  ┌─────────────────────┐  ┌──────────────────────────────┐  │
│  │  추천 엔진 API       │  │  분석/성과 측정 API          │  │
│  │  POST /api/recommend │  │  GET /api/metrics            │  │
│  │  GET  /api/jobs      │  │  GET /api/ab-results         │  │
│  └──────────┬──────────┘  └──────────────────────────────┘  │
└─────────────┼───────────────────────────────────────────────┘
              │
┌─────────────▼───────────────────────────────────────────────┐
│                    추천 엔진 (서비스 레이어)                   │
│  ┌────────────────┐  ┌──────────────┐  ┌────────────────┐   │
│  │ 콘텐츠 기반     │  │  협업 필터링  │  │  하이브리드    │   │
│  │ 필터링         │  │              │  │  앙상블        │   │
│  └────────────────┘  └──────────────┘  └────────────────┘   │
└─────────────┬───────────────────────────────────────────────┘
              │
┌─────────────▼───────────────────────────────────────────────┐
│                       데이터 레이어                           │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │  사용자 DB    │  │  공고/직무 DB │  │  매칭 이력 DB     │  │
│  │  (프로필,     │  │  (스킬,      │  │  (클릭, 지원,     │  │
│  │   스킬 태그)  │  │   요구사항)  │  │   채용 결과)     │  │
│  └──────────────┘  └──────────────┘  └───────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 반응형 레이아웃 전략

### 6.1 Tailwind CSS Breakpoint 정의
| Breakpoint | 최소 너비 | 대상 기기 |
|-----------|---------|---------|
| `sm` | 640px | 대형 스마트폰 (가로) |
| `md` | 768px | 태블릿 |
| `lg` | 1024px | 소형 데스크탑 |
| `xl` | 1280px | 일반 데스크탑 |
| `2xl` | 1536px | 대형 모니터 |

### 6.2 주요 페이지 레이아웃

**모바일 (< 768px)**
```
┌─────────────────┐
│    Header       │
├─────────────────┤
│  검색/필터 (접기)│
├─────────────────┤
│  추천 카드 1    │
├─────────────────┤
│  추천 카드 2    │
├─────────────────┤
│  추천 카드 3    │
└─────────────────┘
```

**데스크탑 (≥ 1024px)**
```
┌──────────────────────────────────────────┐
│               Header + Nav               │
├─────────────┬────────────────────────────┤
│  필터 패널  │  추천 카드 그리드 (2~3열)  │
│  (사이드바) │  ┌────┐ ┌────┐ ┌────┐     │
│  - 직무     │  │카드│ │카드│ │카드│     │
│  - 경력     │  └────┘ └────┘ └────┘     │
│  - 지역     │  ┌────┐ ┌────┐ ┌────┐     │
│  - 기술스택 │  │카드│ │카드│ │카드│     │
│             │  └────┘ └────┘ └────┘     │
└─────────────┴────────────────────────────┘
```

---

## 7. 공통 컴포넌트 목록

| 컴포넌트 | 파일 경로 | 설명 |
|---------|---------|------|
| `Header` | `components/layout/Header.tsx` | 내비게이션, 로그인 상태 표시 |
| `JobCard` | `components/cards/JobCard.tsx` | 공고/프로젝트 카드, 매칭 스코어 포함 |
| `MatchScore` | `components/ui/MatchScore.tsx` | 매칭 점수 시각화 (게이지 바) |
| `FilterPanel` | `components/filters/FilterPanel.tsx` | 직무/경력/지역/스킬 필터 |
| `ChartWidget` | `components/charts/ChartWidget.tsx` | Chart.js 래퍼 컴포넌트 |
| `RecommendList` | `components/recommend/RecommendList.tsx` | 추천 목록, 무한 스크롤 |
| `SkillTag` | `components/ui/SkillTag.tsx` | 스킬 태그 배지 |
| `LoadingSkeleton` | `components/ui/LoadingSkeleton.tsx` | 카드 로딩 스켈레톤 |

---

## 8. 디렉토리 구조

```
career-matching/
├── app/                        # Next.js App Router
│   ├── page.tsx                # 메인 페이지 (추천 목록)
│   ├── jobs/
│   │   └── [id]/page.tsx       # 공고 상세
│   ├── profile/
│   │   └── page.tsx            # 사용자 프로필
│   ├── dashboard/
│   │   └── page.tsx            # 성과 대시보드
│   └── api/
│       ├── recommend/route.ts  # 추천 API
│       ├── metrics/route.ts    # 성과 측정 API
│       └── ab/route.ts         # A/B 테스트 API
├── components/                 # 공통 컴포넌트
│   ├── layout/
│   ├── cards/
│   ├── charts/
│   ├── filters/
│   ├── recommend/
│   └── ui/
├── lib/                        # 유틸리티, 비즈니스 로직
│   ├── recommendation/
│   │   ├── contentBased.ts
│   │   ├── collaborative.ts
│   │   └── hybrid.ts
│   ├── analytics/
│   └── abtest/
├── types/                      # TypeScript 타입 정의
├── hooks/                      # 커스텀 훅
└── docs/challenge/             # 과업별 기획 문서
    ├── 00-overview.md
    ├── 01-data-collection-analysis.md
    ├── 02-recommendation-system.md
    ├── 03-ab-testing-optimization.md
    └── 04-performance-measurement.md
```

---

## 9. 문서 목록

| 문서 | 담당 | 링크 |
|------|------|------|
| 프로젝트 개요 (본 문서) | 팀 공통 | `00-overview.md` |
| 데이터 수집 및 분석 | 팀원 A | `01-data-collection-analysis.md` |
| 추천 시스템 개선 | 팀원 B | `02-recommendation-system.md` |
| A/B 테스트 및 성능 최적화 | 팀원 C | `03-ab-testing-optimization.md` |
| 프로젝트 성과 측정 및 분석 | 팀원 A (겸임) | `04-performance-measurement.md` |
