# 핵심 기능 명세서 (Core PRD)

> 버전: v1.0 | 작성일: 2026-04-01
> 기반 문서: `prd-final-v2.md` (v3.0 통합본)
> 범위: MVP 기능 (P0 + P1) — 총 9개 기능

---

## 문서 구조

┌──────────────────────┬─────────────────────────────────────────────────────┐                                      
  │         섹션         │                        내용                         │
  ├──────────────────────┼─────────────────────────────────────────────────────┤                                      
  │ 1. MVP 기능 전체 맵  │ 우선순위 요약, 페이지-기능 매핑, 기능 의존 관계도   │
  ├──────────────────────┼─────────────────────────────────────────────────────┤                                      
  │ 2. P0 상세 (3개)     │ F-01, F-02, F-03 — 와이어프레임 + AC + API + 이벤트 │                                      
  ├──────────────────────┼─────────────────────────────────────────────────────┤                                      
  │ 3. P1 상세 (6개)     │ F-04~F-06, F-15~F-17 — 동일 상세도                  │                                      
  ├──────────────────────┼─────────────────────────────────────────────────────┤                                      
  │ 4. 데이터 스키마     │ 전체 TypeScript 인터페이스 (복사 가능)              │
  ├──────────────────────┼─────────────────────────────────────────────────────┤                                      
  │ 5. API 라우트        │ MVP 10개 엔드포인트                                 │
  ├──────────────────────┼─────────────────────────────────────────────────────┤                                      
  │ 6. 이벤트 트래킹     │ 11개 이벤트 전체 목록                               │
  ├──────────────────────┼─────────────────────────────────────────────────────┤                                      
  │ 7. Phase별 구현 순서 │ Day 1~4 매핑                                        │
  ├──────────────────────┼─────────────────────────────────────────────────────┤                                      
  │ 8. v2 참고           │ MVP 이후 9개 기능                                   │
  └──────────────────────┴─────────────────────────────────────────────────────┘ 
  
  prd-final-v2.md 대비 추가된 점:
  - 각 기능별 와이어프레임 (텍스트 UI)
  - 가중치 계산 규칙, 레벨 분류 기준, 검토 포인트 유형 등 구현 레벨 상세
  - 페이지-기능 매핑 + 기능 의존 관계도
  - 복사 가능한 전체 TypeScript 스키마

## 1. MVP 기능 전체 맵

### 1.1 기능 우선순위 요약

| 우선순위 | 기능 ID | 기능명 | 대상 | 연결 기회 | Phase |
|---------|--------|-------|------|---------|-------|
| **P0** | F-01 | 매칭 스코어 카드 | 구직자 | O1 | Phase 1 |
| **P0** | F-02 | 경력 레벨 자동 태그 | 구직자 | O1 | Phase 1 |
| **P0** | F-03 | 지원 전 역량 리포트 + AI 탈락 사유 시뮬레이션 | 구직자 | O2, O5 | Phase 2 |
| **P1** | F-04 | 포트폴리오 프로젝트 태그 입력 | 구직자 | O4 | Phase 1 |
| **P1** | F-05 | 조직문화 핏 태그 시스템 | 양면 | O3 | Phase 2 |
| **P1** | F-06 | 자격요건 자동 필터링 + Skill-Fit Score | 기업 | 기업1-2 | Phase 2 |
| **P1** | F-15 | 구직자 역량 진단 + 보완 우선순위 | 구직자 | O5, O7 | Phase 1 |
| **P1** | F-16 | 이력서 개선 가이드 | 구직자 | O7 | Phase 2 |
| **P1** | F-17 | 리스크/검토 필요 포인트 표시 | 기업 | O6 | Phase 2 |

### 1.2 페이지-기능 매핑

| 페이지 (라우트) | 포함 기능 | 대상 사용자 |
|---------------|---------|-----------|
| `/` 랜딩 | 서비스 소개, CTA | 비로그인 |
| `/jobs` 공고 목록 | F-01, F-02, F-05(필터) | 구직자 |
| `/jobs/:id` 공고 상세 | F-03, F-05(태그 표시) | 구직자 |
| `/profile` 구직자 프로필 | F-04, F-15, F-16 | 구직자 |
| `/employer` 기업 대시보드 | F-06, F-17 | 기업 HR |

### 1.3 기능 의존 관계

```
F-15 역량 진단 ──→ F-16 이력서 개선 가이드 (진단 결과 기반 가이드 생성)
       │
       └──→ F-01 매칭 스코어 카드 (진단된 스킬 데이터를 스코어 계산에 활용)
              │
              └──→ F-02 경력 레벨 자동 태그 (스코어 카드 위에 레벨 태그 표시)
                     │
                     └──→ F-03 역량 리포트 (공고 상세에서 갭 분석)

F-04 프로젝트 태그 ──→ F-01 매칭 스코어 (프로젝트 카드가 가중치에 반영)

F-05 문화 태그 ──→ F-01 매칭 스코어 (문화 핏이 필터에 반영)

F-06 자격요건 필터링 ──→ F-17 리스크 포인트 (필터링 결과 위에 검토 포인트 표시)
```

---

## 2. P0 기능 상세 명세

---

### F-01 | 매칭 스코어 카드

| 항목 | 내용 |
|-----|------|
| 우선순위 | **P0** |
| 목적 | 공고 카드에서 적합도를 시각적으로 보여줘 클릭 전 판단 가능 |
| 대상 페르소나 | 이민준 (주니어 FE) |
| 연결 페인포인트 | 구직자 #5 — 수준 안 맞는 공고에 시간 낭비 |
| 페이지 | `/jobs` 공고 목록 |

**사용자 스토리**
> 이민준으로서, 공고 목록을 스크롤하면서 클릭 전에 이 공고가 내 수준에 맞는지 알고 싶다.
> 그래야 맞지 않는 공고를 여는 데 시간을 낭비하지 않을 수 있다.

**기능 설명**
- 추천 공고 카드에 매칭 스코어(0~100점) 게이지 바 표시
- 일치 스킬: `React ✓`, `TypeScript ✓` / 부족 스킬: `AWS ✗` 인라인 배지
- "이 공고는 당신에게 82% 맞습니다. AWS 경험이 있다면 95%." 1줄 요약 문구

**카드 UI 와이어프레임**
```
┌────────────────────────────────────┐
│ 프론트엔드 개발자 · (주)OO기업      │
│ 서울 · 정규직 · 5,000~7,000만 원   │
│ ████████████████░░░░  82%          │
│ ✓ React  ✓ TypeScript  ✗ AWS       │
│ "AWS 경험이 있다면 95%"             │
│ [지원하기]              [저장]      │
└────────────────────────────────────┘
```

**스코어 계산 로직** (`/src/lib/scoring.ts`)
```
matchScore = Σ (스킬 일치 가중치 × 경력 보정 × 프로젝트 복잡도 보정)

- 스킬 일치: 필수 스킬 매칭 비율 (0~60점)
- 경력 보정: 요구 경력 범위 내 ±1년 이내 시 만점 (0~25점)
- 프로젝트 복잡도: F-04 프로젝트 카드 등록 시 가산 (0~15점)
```

**수용 기준 (AC)**
- [ ] 스코어 0~100 범위로 계산되어 카드에 표시된다
- [ ] 일치 스킬은 초록(`#22C55E`), 부족 스킬은 빨간(`#EF4444`) 배지로 구분된다
- [ ] 1줄 요약 문구가 가장 높은 갭 스킬 기준으로 자동 생성된다
- [ ] 신규 공고 등 스코어 없는 경우 배지 없이 정상 표시된다
- [ ] 모바일(< 768px)에서 게이지 바가 정상 렌더링된다

**연결 API**: `POST /api/recommend` → `JobPosting[]` + `CandidateScore[]`

**A/B 연결**: E1 — 숫자형(A) vs 게이지+스킬 배지(B) CTR·지원 전환율 비교

**이벤트 트래킹**
| 이벤트 | 트리거 | 수집 데이터 |
|-------|-------|-----------|
| `job_card_view` | 카드 뷰포트 진입 | job_id, score, ab_variant |
| `job_card_click` | 카드 클릭 | job_id, score, ab_variant |

---

### F-02 | 경력 레벨 자동 태그

| 항목 | 내용 |
|-----|------|
| 우선순위 | **P0** |
| 목적 | 공고 경력 요건 파싱 → 레벨 태그 자동 부착, 탐색 정확도 향상 |
| 대상 페르소나 | 이민준 (주니어 FE) |
| 연결 페인포인트 | 구직자 #5 — 수준 안 맞는 공고 추천 |
| 페이지 | `/jobs` 공고 목록, `/jobs/:id` 공고 상세 |

**사용자 스토리**
> 이민준으로서, 추천 목록에서 "주니어 적합" 태그가 붙은 공고만 모아보고 싶다.
> 시니어 공고를 열어봤다가 실망하는 상황을 없애고 싶다.

**기능 설명**
- `requiredExperience` 파싱 → 레벨 태그 자동 부착
- 사용자 경력과 공고 레벨 1단계 이상 차이 시 "레벨 불일치" 경고 배지
- 필터 패널에 레벨 필터 추가 (중복 선택 가능)

**레벨 분류 기준** (`/src/lib/levelTag.ts`)

| 경력 범위 | 레벨 태그 | 색상 |
|----------|---------|------|
| 0년 | `[신입]` | `#6366F1` (인디고) |
| 1~2년 | `[주니어]` | `#22C55E` (그린) |
| 3~6년 | `[미들]` | `#F59E0B` (앰버) |
| 7년+ | `[시니어]` | `#EF4444` (레드) |

**레벨 불일치 경고 규칙**

| 사용자 레벨 | 공고 레벨 | 결과 |
|-----------|---------|------|
| 주니어 | 시니어 | ⚠ "레벨 불일치" 경고 배지 |
| 주니어 | 미들 | ⚠ "레벨 불일치" 경고 배지 |
| 주니어 | 주니어 | 정상 표시 |
| 미들 | 신입 | 정상 표시 (하향은 경고 없음) |

**수용 기준 (AC)**
- [ ] 경력 조건 경계값: 0년=신입, 1~2년=주니어, 3~6년=미들, 7년+=시니어로 정확히 분류된다
- [ ] 사용자 레벨과 1단계 이상 차이(상향) 시 경고 배지가 표시된다
- [ ] 레벨 필터 선택 시 해당 레벨 공고만 노출된다
- [ ] 중복 레벨 선택 시 OR 조건으로 필터링된다

---

### F-03 | 지원 전 역량 리포트 + AI 탈락 사유 시뮬레이션

| 항목 | 내용 |
|-----|------|
| 우선순위 | **P0** |
| 목적 | 지원 전 합격자 평균 대비 갭 시각화 + AI가 부족 요소를 사전 피드백 |
| 대상 페르소나 | 이민준 (주니어 FE) |
| 연결 페인포인트 | 구직자 #1, #3 — 내 수준 파악 불가, 탈락 피드백 부재 |
| 제품 원칙 | 탈락 판정이 아닌 "보완 방향 중심 피드백" |
| 페이지 | `/jobs/:id` 공고 상세 (사이드 패널) |

**사용자 스토리**
> 이민준으로서, 공고에 지원하기 전에 합격자 평균과 비교해 어느 부분이 부족한지 알고 싶다.
> 그래야 지원 여부를 더 잘 판단하고, 탈락하더라도 무엇을 보완해야 하는지 알 수 있다.

**기능 구성**

**A. 역량 리포트** (사이드 패널)
- "지원하기" 버튼 클릭 시 사이드 패널 오픈
- 합격자 평균 스킬 프로필 vs 내 프로필 바 차트 비교
- 갭 항목별 표시: "React — 합격자 평균 3년 / 나 1년 (갭 2년)"
- 보완 추천: "이 공고 합격자들이 많이 보완한 스킬: TypeScript, 사이드 프로젝트 경험"

**리포트 와이어프레임**
```
┌─ 역량 리포트 ──────────────────────┐
│                                    │
│  합격자 평균 vs 내 프로필            │
│                                    │
│  React     ████████░░  3년         │
│  (나)      ███░░░░░░░  1년  갭 2년  │
│                                    │
│  TypeScript ██████░░░░  2년        │
│  (나)       █████░░░░░  1.5년      │
│                                    │
│  AWS        ████░░░░░░  2년        │
│  (나)       ░░░░░░░░░░  0년  갭 2년 │
│                                    │
│  💡 보완 추천                       │
│  "합격자들이 가장 많이 보완한 스킬:   │
│   TypeScript, 사이드 프로젝트 경험"  │
│                                    │
│  [AI 탈락 시뮬레이션 보기]           │
│  [지원하기]  [나중에 지원]           │
└────────────────────────────────────┘
```

**B. AI 탈락 사유 시뮬레이션**
- 공고 JD와 내 이력서를 AI가 대조하여 부족 키워드·경험 분석
- 출력 예시: "이 공고는 AWS 인프라 경험을 중시하는데, 현재 이력서에는 인프라 관련 언급이 없습니다."
- 반드시 **"AI 추정 결과"** 레이블 표시
- 리포트 확인 후 `지원` 또는 `보완 후 재도전` 선택

**수용 기준 (AC)**
- [ ] 역량 리포트가 지원 버튼 클릭 시 1초 이내에 로드된다
- [ ] 합격자 평균 데이터 미보유 공고는 "데이터 수집 중" 안내를 표시한다
- [ ] AI 시뮬레이션 결과가 공고 JD의 요구사항 키워드와 이력서를 비교하여 생성된다
- [ ] AI 시뮬레이션 결과에 "AI 추정 결과" 레이블이 반드시 표시된다
- [ ] 리포트 확인 후 지원 완료 시 `viewed_report=true` 이벤트가 기록된다

**연결 API**
| API | 메서드 | 설명 |
|-----|-------|------|
| `/api/jobs/:id/report` | GET | 합격자 평균 vs 내 프로필 갭 데이터 |
| `/api/jobs/:id/simulate` | POST | AI 탈락 사유 시뮬레이션 피드백 |

**A/B 연결**: E2 — 리포트 노출(B) vs 미노출(A) 재방문율·재지원 수 비교

**이벤트 트래킹**
| 이벤트 | 트리거 | 수집 데이터 |
|-------|-------|-----------|
| `apply_start` | 지원하기 버튼 클릭 | job_id, viewed_report |
| `report_viewed` | 역량 리포트 열람 | job_id, gap_count |
| `ai_simulation_viewed` | AI 시뮬레이션 조회 | job_id, gap_keywords |
| `apply_complete` | 지원 완료 | job_id, time_spent |

---

## 3. P1 기능 상세 명세

---

### F-04 | 포트폴리오 프로젝트 태그 입력

| 항목 | 내용 |
|-----|------|
| 우선순위 | **P1** |
| 목적 | 시니어 경력을 구조화하여 추천 가중치에 반영, 텍스트 이력서 한계 극복 |
| 대상 페르소나 | 박지현 (시니어 UX) |
| 연결 페인포인트 | 구직자 #4 — 포트폴리오 차별화 불가 |
| 페이지 | `/profile` 구직자 프로필 |

**사용자 스토리**
> 박지현으로서, Figma 링크와 출시 서비스 URL 외에 내 프로젝트 경험을 구조적으로 입력하고 싶다.
> 그래야 내 7년 경력이 3년차와 다르게 취급될 수 있다.

**기능 설명**
- 프로필 내 "프로젝트 카드" 섹션 (최대 5개)
- 입력 필드: `기술 스택` / `역할` (리드/팀원/단독) / `팀 규모` / `기간` / `URL (옵션)`
- 프로젝트 카드 등록 시 추천 알고리즘 가중치 +0.1 부여
- 역할="리드" + 팀 규모 5인 이상 시 경력 복잡도 점수 가산

**프로젝트 카드 입력 와이어프레임**
```
┌─ 프로젝트 카드 #1 ─────────────────┐
│                                    │
│  기술 스택: [Figma] [React] [+추가] │
│  역할:     ○ 리드  ● 팀원  ○ 단독  │
│  팀 규모:  [6] 명                   │
│  기간:     [8] 개월                 │
│  URL:      [figma.com/...       ]  │
│                                    │
│  [삭제]                [저장]       │
└────────────────────────────────────┘
│  [+ 프로젝트 카드 추가] (최대 5개)   │
```

**가중치 계산 규칙**

| 조건 | 가산 점수 |
|-----|---------|
| 프로젝트 카드 1개 이상 등록 | +0.1 (기본) |
| 역할 = "리드" | +0.05 추가 |
| 역할 = "리드" + 팀 규모 ≥ 5인 | +0.1 추가 |
| 기간 ≥ 6개월 | +0.05 추가 |
| URL 포함 | +0.03 추가 |

**수용 기준 (AC)**
- [ ] 프로젝트 카드 1개 이상 등록 시 매칭 스코어가 미등록 대비 변경된다
- [ ] 역할 "리드" 선택 시 경력 복잡도 점수에 가산이 적용된다
- [ ] 카드 등록/수정/삭제가 모두 가능하다
- [ ] 최대 5개 초과 시 추가 버튼 비활성화 + 안내 메시지 표시
- [ ] 기술 스택 입력은 자동완성(태그 형태)으로 동작한다

---

### F-05 | 조직문화 핏 태그 시스템

| 항목 | 내용 |
|-----|------|
| 우선순위 | **P1** |
| 목적 | 공고 문구 대신 검증된 태그로 입사 전 실제 조직문화 판단 |
| 대상 페르소나 | 박지현 (시니어 UX) |
| 연결 페인포인트 | 구직자 #2 — 조직문화 검증 불가 |
| 페이지 | `/jobs` (필터), `/jobs/:id` (태그 표시) |

**사용자 스토리**
> 박지현으로서, 공고 페이지에서 "수평적 문화"라는 문구 말고, 실제 의사결정 방식을 데이터로 보고 싶다.
> 그래야 입사 후 실망하는 경험을 반복하지 않을 수 있다.

**기능 구성**

**A. 기업 측 — 공고 등록 시 태그 선택**
- 문화 핏 태그 선택 UI (최대 5개, 선택 필수 아님)
- 태그 선택 시 공고 노출 가산점 적용

**문화 태그 풀 (20개)**
| 카테고리 | 태그 |
|---------|------|
| 의사결정 | `빠른 의사결정`, `합의 기반 결정`, `데이터 기반 결정` |
| 근무 형태 | `주 2회 재택`, `풀 리모트`, `유연 출퇴근`, `선택적 근로시간제` |
| 개발 문화 | `코드 리뷰 문화`, `페어 프로그래밍`, `TDD 지향` |
| 조직 문화 | `수평적 피드백`, `자율과 책임`, `성과 중심 평가` |
| 성장 지원 | `교육비 지원`, `컨퍼런스 참석`, `사이드 프로젝트 허용` |
| 제품 | `디자이너 주도 제품`, `엔지니어 주도 제품`, `PM 주도 제품` |

**B. 구직자 측 — 공고 상세 태그 표시**
- 기업 선택 문화 태그 배지 표시
- 태그별 재직자 평점(1~5점, 익명) 마우스오버 시 툴팁 표시
- "이 태그와 일치하는 공고만 보기" 필터 연동

**태그 표시 와이어프레임**
```
조직문화 핏
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ 코드 리뷰 문화 │ │ 주 2회 재택   │ │ 수평적 피드백  │
│ ★★★★☆ 4.2   │ │ ★★★★★ 4.8   │ │ ★★★☆☆ 3.5   │
└──────────────┘ └──────────────┘ └──────────────┘
```

**수용 기준 (AC)**
- [ ] 태그 없는 공고도 공고 상세 정상 표시 (태그 섹션만 비표시)
- [ ] 문화 태그 필터 선택 시 해당 태그 보유 공고만 노출된다
- [ ] 태그 클릭 시 재직자 평점 요약이 툴팁으로 표시된다
- [ ] 태그 최대 5개 초과 선택 시 경고 표시

**연결 API**: `GET /api/jobs/:id/culture` → `CultureTag[]`

**A/B 연결**: E3 — 문화 태그 유무에 따른 공고 지원 전환율 비교

**이벤트 트래킹**
| 이벤트 | 트리거 | 수집 데이터 |
|-------|-------|-----------|
| `culture_tag_filter` | 문화 태그 필터 적용 | tag_list |

---

### F-06 | 자격요건 자동 필터링 + Skill-Fit Score (기업용)

| 항목 | 내용 |
|-----|------|
| 우선순위 | **P1** |
| 목적 | 200명+ 지원자 중 유효 후보를 자동 분류 + 기술 적합도 점수 제공 |
| 대상 페르소나 | 최동훈 (개발팀장), 한정원 (HR 리드) |
| 연결 페인포인트 | 기업 #2 (대량 스크리닝), #3 (키워드 매칭 부정확) |
| 페이지 | `/employer` 기업 대시보드 |

**사용자 스토리**
> 한정원으로서, 필수 조건을 충족하지 못한 지원자는 자동으로 걸러지길 원한다.
> 최동훈으로서, 키워드가 아닌 실무 맥락 기반으로 기술 적합도를 확인하고 싶다.

**기능 구성**

**A. 자격요건 자동 필터링**
- 공고 등록 시 HR이 필수 조건 설정: 최소 경력 연수, 필수 스킬, 자격증
- 지원자 수신 시 자동 판별 → 3단계 분류

| 분류 | 기준 | UI 표시 |
|-----|------|--------|
| `적합 후보` | 필수 조건 100% 충족 | 🟢 기본 탭 |
| `검토 필요` | 필수 조건 70~99% 충족 | 🟡 별도 탭 |
| `조건 미충족` | 필수 조건 70% 미만 | 🔴 접힘 탭 (수동 열람) |

**B. Skill-Fit Score**
- 단순 키워드 매칭이 아닌 이력서 내 프로젝트 문맥 분석
- "우리 팀 기술 스택과 얼마나 호환되는지" 0~100점 산출
- 점수 근거 요약: "프로젝트 A에서 React 3년 사용 확인됨"

**기업 대시보드 와이어프레임**
```
┌─ 지원자 관리 ──────────────────────────────────┐
│                                                │
│  [적합 후보 (8)] [검토 필요 (7)] [미충족 (5)]    │
│                                                │
│  ┌────────────────────────────────────────┐    │
│  │ 김OO · 3년차 FE                        │    │
│  │ Skill-Fit: ████████░░ 85점             │    │
│  │ 근거: "React 3년, TypeScript 2년 확인"  │    │
│  │ ⚠ 확인 필요: 12개월 내 2회 이직         │    │
│  │ [상세보기]  [면접 초대]  [보류]          │    │
│  └────────────────────────────────────────┘    │
└────────────────────────────────────────────────┘
```

**수용 기준 (AC)**
- [ ] 필수 조건 설정: 최소 경력(년), 필수 스킬(복수), 자격증(텍스트) 3종 이상 지원
- [ ] 자동 분류 정확도: 테스트 기준 `적합/미충족` 판별 일치율 95% 이상
- [ ] Skill-Fit Score가 키워드 매칭이 아닌 프로젝트 문맥 기반으로 산출된다
- [ ] HR이 조건 미충족 지원자를 수동 `적합` 재분류할 수 있다
- [ ] 각 분류 탭에 지원자 수 실시간 표시된다
- [ ] Skill-Fit Score에 점수 근거 요약이 1줄 이상 포함된다

**연결 API**
| API | 메서드 | 설명 |
|-----|-------|------|
| `/api/employer/filter` | POST | 지원자 자격요건 자동 필터링 |
| `/api/employer/skill-fit` | POST | Skill-Fit Score 산출 |

**이벤트 트래킹**
| 이벤트 | 트리거 | 수집 데이터 |
|-------|-------|-----------|
| `skill_fit_score_viewed` | HR이 Skill-Fit Score 조회 | candidate_id, score |

---

### F-15 | 구직자 역량 진단 + 보완 우선순위

| 항목 | 내용 |
|-----|------|
| 우선순위 | **P1** |
| 목적 | 구직자가 현재 역량 수준을 구조적으로 파악하고 보완 방향을 알 수 있게 함 |
| 대상 페르소나 | 이민준 (주니어 FE) |
| 연결 페인포인트 | 구직자 #1, #3 — 내 수준 파악 불가, 학습 우선순위 불명확 |
| 제품 원칙 | 점수만이 아닌 강점+부족점 함께 설명, 개선 방향 중심 피드백 |
| 출처 | yeseul-prd |
| 페이지 | `/profile` 구직자 프로필 |

**사용자 스토리**
> 이민준으로서, 내가 지금 시장에서 어느 수준인지 감이 아니라 데이터로 알고 싶다.
> 그래야 뭘 먼저 준비해야 하는지 판단하고, 커뮤니티 정보에 의존하지 않을 수 있다.

**기능 설명**
- 프로필 작성 완료 후 목표 직무 선택 → 역량 진단 자동 실행
- 업로드된 이력서에서 기술 스택, 프로젝트 경험, 성과 표현 분석
- 결과: 강점 항목 + 부족 역량 2~3개를 핵심 항목으로 압축 표시
- 각 부족 항목별 "왜 부족한지" 설명 + 보완 방향 3관점 제시

**역량 진단 결과 와이어프레임**
```
┌─ 역량 진단 결과 ───────────────────┐
│                                    │
│  🎯 목표 직무: 프론트엔드 개발자     │
│                                    │
│  ✅ 강점                            │
│  • React 기본기 보유 (1년 실무)      │
│  • 컴포넌트 설계 경험                │
│                                    │
│  ⚠ 보완 필요 (우선순위순)            │
│                                    │
│  1. TypeScript                     │
│     이유: 목표 직무 공고의 85%가 요구 │
│     📚 학습: TS 공식 문서 + 실습     │
│     🛠 프로젝트: 기존 JS → TS 마이그  │
│     📝 이력서: TS 활용 프로젝트 추가  │
│                                    │
│  2. 테스트 코드 작성                 │
│     이유: 시니어 공고의 60%가 우대    │
│     📚 학습: Jest + RTL 기초         │
│     🛠 프로젝트: 기존 프로젝트에 적용  │
│                                    │
│  [이력서 개선 가이드 보기 →]          │
└────────────────────────────────────┘
```

**보완 방향 3관점**

| 관점 | 설명 | 아이콘 |
|-----|------|-------|
| 학습 (Learning) | 학습 자료·강의·문서 방향 | 📚 |
| 프로젝트 (Project) | 사이드 프로젝트·업무 적용 방향 | 🛠 |
| 이력서 수정 (Resume) | 이력서 표현 보완 방향 | 📝 |

**수용 기준 (AC)**
- [ ] 목표 직무 선택 UI에서 직무 카테고리 5개 이상 선택 가능하다
- [ ] 진단 결과에 강점과 부족점이 분리되어 표시된다
- [ ] 부족 역량은 2~3개로 압축되며 각각 "왜 부족한지" 설명이 포함된다
- [ ] 보완 방향은 학습/프로젝트/이력서 수정 3가지 관점 중 1개 이상 제시된다
- [ ] 진단 결과에 "AI 분석 결과" 레이블이 표시된다
- [ ] 진단 완료 시 `diagnosis_completed` 이벤트가 기록된다

**연결 API**: `POST /api/diagnosis` → `DiagnosisResult`

**이벤트 트래킹**
| 이벤트 | 트리거 | 수집 데이터 |
|-------|-------|-----------|
| `diagnosis_completed` | 역량 진단 완료 | user_id, target_job, gap_count |

---

### F-16 | 이력서 개선 가이드

| 항목 | 내용 |
|-----|------|
| 우선순위 | **P1** |
| 목적 | 이력서의 모호하거나 부족한 표현을 탐지하여 구체적 수정 방향 제시 |
| 대상 페르소나 | 이민준 (주니어 FE) |
| 연결 페인포인트 | 구직자 #1 — 뭘 보완해야 하는지 모름 |
| 제품 원칙 | 탈락 원인을 추정 가능한 형태로 제공, 개선 예시 동반 |
| 출처 | yeseul-prd |
| 페이지 | `/profile` 구직자 프로필 (F-15 하단 연결) |

**사용자 스토리**
> 이민준으로서, 내 이력서의 어느 부분이 약한지 구체적으로 알고 싶다.
> 그래야 다음 지원 전에 문서를 개선할 수 있다.

**탐지 유형 및 개선 예시**

| 유형 | 탐지 기준 | 원문 예시 | 개선 예시 |
|-----|---------|---------|---------|
| `vague_achievement` | 성과가 드러나지 않는 표현 | "프론트엔드 개발을 담당했습니다" | "React 기반 주문 페이지를 개발하여 전환율을 12% 개선했습니다" |
| `skill_listing` | 기술 나열형 문장 | "React, TypeScript, Redux 사용" | "React + TypeScript로 디자인 시스템 10개 컴포넌트를 구축하여 팀 개발 속도 30% 향상" |
| `irrelevant_description` | 목표 직무와 연결되지 않는 서술 | FE 지원인데 "서버 장비 관리 담당" | 해당 항목 제거 또는 FE 연관 경험으로 대체 권장 |

**이력서 개선 가이드 와이어프레임**
```
┌─ 이력서 개선 가이드 ──────────────────┐
│                                      │
│  📝 AI 분석 결과                      │
│                                      │
│  수정 포인트 3건 발견                   │
│                                      │
│  1. 성과 미표현 (경력기술서 2번째 항목)  │
│     원문: "프론트엔드 개발을 담당"       │
│     문제: 구체적 성과가 드러나지 않음     │
│     💡 개선 예시:                      │
│     "React 기반 주문 페이지를 개발하여   │
│      전환율을 12% 개선"                │
│                                      │
│  2. 기술 나열 (스킬 섹션)               │
│     원문: "React, TypeScript 사용"     │
│     문제: 어떤 맥락에서 사용했는지 불명   │
│     💡 개선 예시:                      │
│     "React+TS로 디자인 시스템 10개      │
│      컴포넌트 구축, 개발 속도 30% 향상" │
│                                      │
│  [도움이 됐어요 👍]  [아니에요 👎]      │
└──────────────────────────────────────┘
```

**수용 기준 (AC)**
- [ ] 이력서 분석 후 수정 포인트가 3개 이상 항목별로 표시된다
- [ ] 각 수정 포인트에 원문 + 문제 설명 + 개선 예시 문장이 포함된다
- [ ] 분석 결과에 "AI 분석 결과" 레이블이 표시된다
- [ ] "도움됨/아님" 피드백 버튼이 포함된다
- [ ] 모바일(< 768px)에서 개선 가이드가 정상 렌더링된다

**연결 API**: `POST /api/resume/guide` → `ResumeGuideResult`

**이벤트 트래킹**
| 이벤트 | 트리거 | 수집 데이터 |
|-------|-------|-----------|
| `resume_guide_viewed` | 이력서 개선 가이드 열람 | user_id, fix_point_count |

---

### F-17 | 리스크/검토 필요 포인트 표시

| 항목 | 내용 |
|-----|------|
| 우선순위 | **P1** |
| 목적 | 지원자의 이직 패턴·경력 맥락 중 면접에서 추가 확인이 필요한 부분 표시 |
| 대상 페르소나 | 최동훈 (개발팀장) |
| 연결 페인포인트 | 기업 #1 (평가 기준 괴리), 최동훈 페인포인트 2 (이직 리스크 판단 불가) |
| 제품 원칙 | 탈락 판정이 아니라 "확인 필요" 신호로 제공 |
| 출처 | yeseul-prd |
| 페이지 | `/employer` 기업 대시보드 (F-06 위에 표시) |

**사용자 스토리**
> 최동훈으로서, 6개월 단위 이직 3번인 지원자가 왔을 때 면접 전에 확인할 포인트를 미리 알고 싶다.
> 그래야 면접 시간을 효율적으로 쓸 수 있다.

**검토 포인트 유형**

| 유형 | 탐지 기준 | 표시 예시 |
|-----|---------|---------|
| `short_tenure` | 최근 3년 내 재직 12개월 미만 2회 이상 | "최근 3회 이직이 모두 12개월 이내 — 사유 확인 권장" |
| `skill_mismatch` | 필수 스킬 보유하나 실무 경험 없음 | "React 기재했으나 관련 프로젝트 경험 미확인 — 실무 수준 확인 권장" |
| `career_change` | 직군 전환 이력 | "이전 직군(백엔드)에서 프론트엔드로 전환 — 전환 동기 확인 권장" |

**수용 기준 (AC)**
- [ ] 검토 포인트가 있는 지원자에게 주황색(`#F59E0B`) "확인 필요" 배지가 표시된다
- [ ] 각 검토 포인트에 1줄 이상의 설명이 포함된다
- [ ] 검토 포인트가 없는 지원자에게는 배지가 표시되지 않는다
- [ ] HR이 검토 포인트를 "확인 완료"로 처리할 수 있다
- [ ] "확인 필요"는 자동 탈락 판정에 사용되지 않는다 (수동 재분류 가능)

**연결 API**: `GET /api/employer/risk-points` → `RiskPoint[]`

**이벤트 트래킹**
| 이벤트 | 트리거 | 수집 데이터 |
|-------|-------|-----------|
| `risk_point_reviewed` | HR이 검토 포인트 확인 완료 | candidate_id, risk_type |

---

## 4. MVP 데이터 스키마

```typescript
// /src/types/index.ts

// === 기본 타입 ===

export interface Skill {
  name: string;
  yearsOfExperience?: number;
}

export type LevelTag = 'entry' | 'junior' | 'middle' | 'senior';
export type ABVariant = 'A' | 'B';
export type FilterCategory = 'suitable' | 'review' | 'unqualified';

// === 구직자 ===

export interface UserProfile {
  id: string;
  name: string;
  skills: Skill[];
  experienceYears: number;
  projects: ProjectCard[];              // F-04
  preferences: JobPreference;
  portfolioUrls: string[];
  diagnosisResult?: DiagnosisResult;    // F-15
}

export interface ProjectCard {          // F-04
  id: string;
  techStack: string[];
  role: 'lead' | 'member' | 'solo';
  teamSize: number;
  durationMonths: number;
  url?: string;
  description?: string;
}

export interface JobPreference {
  levelTags: LevelTag[];
  cultureTags: string[];
  locationPreference?: string;
}

export interface DiagnosisResult {      // F-15
  targetJob: string;
  strengths: string[];
  weaknesses: WeaknessItem[];
  marketValueScore?: number;
}

export interface WeaknessItem {         // F-15
  skill: string;
  reason: string;
  improvementDirection: 'learning' | 'project' | 'resume';
  suggestion: string;
}

export interface ResumeGuideResult {    // F-16
  fixPoints: ResumeFixPoint[];
}

export interface ResumeFixPoint {       // F-16
  originalText: string;
  issue: 'vague_achievement' | 'skill_listing' | 'irrelevant_description';
  suggestion: string;
  exampleText: string;
}

// === 공고 ===

export interface JobPosting {
  id: string;
  title: string;
  company: string;
  location: string;
  employmentType: string;
  salaryRange: string;
  requiredSkills: Skill[];
  requiredExperience: { min: number; max?: number };
  levelTag: LevelTag;                   // F-02
  cultureTags: CultureTag[];            // F-05
  mustHaveConditions: FilterCondition;  // F-06
  description: string;
  postedAt: string;
}

export interface CultureTag {           // F-05
  id: string;
  label: string;
  rating?: number;                      // 재직자 평점 1~5
}

export interface FilterCondition {      // F-06
  minExperienceYears?: number;
  requiredSkills: string[];
  requiredCertifications?: string[];
}

// === 매칭/점수 ===

export interface CandidateScore {
  userId: string;
  jobId: string;
  matchScore: number;                   // 0~100 구직자용 (F-01)
  skillFitScore: number;                // 0~100 기업용 (F-06)
  matchedSkills: Skill[];
  missingSkills: Skill[];
  aiSimulationFeedback: string;         // F-03
  levelMismatch: boolean;               // F-02
  riskPoints?: RiskPoint[];             // F-17
}

export interface RiskPoint {            // F-17
  type: 'short_tenure' | 'skill_mismatch' | 'career_change';
  description: string;
  reviewed: boolean;
}

// === 기업 ===

export interface CandidateApplication {
  id: string;
  userId: string;
  jobId: string;
  candidateScore: CandidateScore;
  filterCategory: FilterCategory;       // F-06
  appliedAt: string;
}

// === A/B 테스트 ===

export interface ABTestConfig {
  experimentId: string;
  variant: ABVariant;
}
```

---

## 5. MVP API 라우트

| 엔드포인트 | 메서드 | 기능 ID | 설명 | 반환 |
|-----------|-------|--------|------|------|
| `/api/recommend` | POST | F-01, F-02 | 사용자 프로필 기반 공고 추천 | `JobPosting[]` + `CandidateScore[]` |
| `/api/jobs/:id/report` | GET | F-03 | 합격자 평균 vs 내 프로필 갭 | 갭 데이터, 보완 추천 |
| `/api/jobs/:id/simulate` | POST | F-03 | AI 탈락 사유 시뮬레이션 | `aiSimulationFeedback: string` |
| `/api/jobs/:id/culture` | GET | F-05 | 문화 태그 + 재직자 평점 | `CultureTag[]` |
| `/api/diagnosis` | POST | F-15 | 구직자 역량 진단 + 보완 우선순위 | `DiagnosisResult` |
| `/api/resume/guide` | POST | F-16 | 이력서 개선 가이드 분석 | `ResumeGuideResult` |
| `/api/employer/filter` | POST | F-06 | 지원자 자격요건 자동 필터링 | `CandidateApplication[]` |
| `/api/employer/skill-fit` | POST | F-06 | Skill-Fit Score 산출 | `skillFitScore`, 근거 요약 |
| `/api/employer/risk-points` | GET | F-17 | 리스크/검토 필요 포인트 | `RiskPoint[]` |
| `/api/ab` | POST | E1 | A/B 변형 할당 및 이벤트 로그 | `ABVariant` |

> **MVP 구현 방식**: 모든 API 라우트는 실제 DB가 아닌 Mock JSON 파일을 읽어 응답 반환

---

## 6. MVP 이벤트 트래킹 전체 목록

| 이벤트 | 트리거 | 기능 ID | 수집 데이터 |
|-------|-------|--------|-----------|
| `job_card_view` | 카드 뷰포트 진입 | F-01 | job_id, score, ab_variant |
| `job_card_click` | 카드 클릭 | F-01 | job_id, score, ab_variant |
| `apply_start` | 지원하기 버튼 클릭 | F-03 | job_id, viewed_report |
| `apply_complete` | 지원 완료 | F-03 | job_id, time_spent |
| `report_viewed` | 역량 리포트 열람 | F-03 | job_id, gap_count |
| `ai_simulation_viewed` | AI 시뮬레이션 조회 | F-03 | job_id, gap_keywords |
| `culture_tag_filter` | 문화 태그 필터 적용 | F-05 | tag_list |
| `skill_fit_score_viewed` | HR이 Skill-Fit Score 조회 | F-06 | candidate_id, score |
| `diagnosis_completed` | 구직자 역량 진단 완료 | F-15 | user_id, target_job, gap_count |
| `resume_guide_viewed` | 이력서 개선 가이드 열람 | F-16 | user_id, fix_point_count |
| `risk_point_reviewed` | HR이 검토 포인트 확인 완료 | F-17 | candidate_id, risk_type |

---

## 7. Phase별 구현 순서

### Phase 0: 환경 설정 및 문서화 (Day 1 전반)
- 개발 환경 세팅 (Next.js 14, TypeScript, Tailwind CSS)
- TypeScript 타입 정의 (`/src/types/index.ts`) — 위 스키마 기준
- Mock 데이터 작성 (jobs 30건, users 5건, candidates 20건)
- API 라우트 기본 골격

### Phase 1: 핵심 UI + 구직자 탐색 (Day 1 후반 ~ Day 2)
- **F-01** 매칭 스코어 카드 — `/jobs` 페이지 핵심
- **F-02** 경력 레벨 자동 태그 — 필터 포함
- **F-04** 포트폴리오 프로젝트 태그 입력 — `/profile` 페이지
- **F-15** 구직자 역량 진단 — `/profile` 페이지
- 공통 레이아웃 + 랜딩 페이지 + 공고 상세 페이지

### Phase 2: 차별화 기능 + 기업 대시보드 (Day 3)
- **F-03** 역량 리포트 + AI 시뮬레이션 — `/jobs/:id` 사이드 패널
- **F-05** 조직문화 핏 태그 — 공고 등록/상세/필터
- **F-06** 자격요건 필터링 + Skill-Fit Score — `/employer` 대시보드
- **F-16** 이력서 개선 가이드 — `/profile` (F-15 하단)
- **F-17** 리스크 포인트 — `/employer` (F-06 위)
- A/B 테스트 E1 로직

### Phase 3: QA + 최적화 + 배포 (Day 4)
- Lighthouse ≥ 90 최적화
- 반응형 전 구간 (360px, 768px, 1280px, 1920px) 확인
- 접근성 (WCAG 2.1 AA) 검증
- 에러 상태 UI (빈 상태, 로딩, 에러)
- Vercel 배포

---

## 8. v2 기능 목록 (참고)

> MVP 이후 우선순위로 개발 예정. 상세 명세는 `prd-final-v2.md` 참조.

| 기능 ID | 기능명 | 연결 기회 |
|--------|-------|---------|
| F-07 | AI 기술 스택 갭 분석 + 학습 로드맵 | O5 |
| F-08 | 탈락 후 스킬 갭 리포트 자동 발송 | O2 |
| F-09 | 포트폴리오 URL 자동 파싱 | O4 |
| F-10 | 이직 패턴 분석기 (Retention Insight) | O6 |
| F-11 | AI 면접 검증 질문 자동 생성 | O6 |
| F-12 | 현업·HR 이원화 평가 보드 | 기업1-1 |
| F-13 | 시장 포지셔닝 리포트 | O5 |
| F-14 | 재직자 익명 문화 평점 | O3 |
| F-18 | 면접 우선순위 후보 추천 | 기업1-2 |
