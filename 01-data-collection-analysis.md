# 과업 1. 데이터 수집 및 분석

> 담당: 팀원 A (과업 4 성과 측정 겸임)
> 참고 서비스: [커리어데이](https://www.careerday.jobs)
> 개발 기간: 4일 MVP — Day 1 오후까지 목 데이터 완성 목표

---

## 1. 개요 및 목적

### 1.1 과업 목적
커리어 매칭 추천 시스템의 품질을 높이기 위해 필요한 데이터를 정의하고 수집·전처리·분석하는 체계를 설계합니다. 커리어데이의 매칭 구조를 기반으로 어떤 데이터가 추천 알고리즘에 핵심 입력값이 되는지 명확히 합니다.

### 1.2 과업 범위
- 수집할 데이터 항목 정의 및 스키마 설계
- 유사 서비스 추천 방식 벤치마크
- 데이터 전처리 파이프라인 설계
- 핵심 분석 항목 및 시각화 계획

---

## 2. 데이터 소스 정의

### 2.1 수집 데이터 항목 (커리어데이 기준)

#### 구직자/전문가 프로필
| 항목 | 설명 | 예시 |
|------|------|------|
| `user_id` | 사용자 고유 식별자 | UUID |
| `skills` | 보유 기술 스택 태그 배열 | `["React", "TypeScript", "Node.js"]` |
| `experience_years` | 경력 연수 | `3` |
| `job_category` | 희망 직무 카테고리 | `"프론트엔드 개발"` |
| `location` | 선호 근무지 | `"서울 강남"`, `"원격"` |
| `salary_range` | 희망 연봉 범위 | `{ min: 4000, max: 6000 }` (만원) |
| `portfolio_count` | 포트폴리오 수 | `5` |
| `activity_score` | 최근 30일 활동 점수 | `0.0 ~ 1.0` |

#### 기업 공고 / 프로젝트
| 항목 | 설명 | 예시 |
|------|------|------|
| `job_id` | 공고 고유 식별자 | UUID |
| `required_skills` | 필수 기술 스택 | `["Python", "ML", "FastAPI"]` |
| `preferred_skills` | 우대 기술 스택 | `["Docker", "AWS"]` |
| `experience_level` | 요구 경력 수준 | `"신입"`, `"3~5년"`, `"5년 이상"` |
| `job_type` | 고용 형태 | `"정규직"`, `"프리랜서"`, `"프로젝트"` |
| `industry` | 산업군 | `"IT/소프트웨어"`, `"핀테크"` |
| `salary_offered` | 제시 급여 | `{ type: "협의", range: [5000, 8000] }` |
| `location` | 근무 위치 | `"서울 마포"`, `"전국(원격)"` |

#### 매칭 이력 및 행동 데이터
| 항목 | 설명 | 데이터 타입 |
|------|------|-----------|
| `view_events` | 공고 조회 이벤트 | `{ user_id, job_id, timestamp, duration_sec }` |
| `bookmark_events` | 찜하기 이벤트 | `{ user_id, job_id, timestamp }` |
| `apply_events` | 지원 이벤트 | `{ user_id, job_id, timestamp, status }` |
| `match_results` | 매칭 결과 | `{ match_id, user_id, job_id, score, outcome }` |
| `hire_results` | 채용 완료 결과 | `{ match_id, hired_at, feedback_score }` |

### 2.2 유사 서비스 추천 방식 벤치마크

| 서비스 | 추천 방식 | 핵심 입력 데이터 | 특징 |
|--------|---------|---------------|------|
| **LinkedIn** | 하이브리드 (협업 + 콘텐츠) | 연결망, 스킬, 직무 이력, 행동 데이터 | "당신과 유사한 사람들이 지원한 공고" |
| **원티드(Wanted)** | 콘텐츠 기반 + AI | 이력서 키워드, 지원 이력, 스킬 매칭 | 합격 예측 점수 표시 |
| **로켓펀치(Rocketpunch)** | 키워드 기반 매칭 | 프로필 태그, 팀/회사 스택 | 스타트업 특화, 팀 핏 강조 |
| **커리어데이** | 매칭 스코어 기반 | 전문가 포트폴리오, 프로젝트 요구사항 | B2B 프로젝트 중심, 전문가 검증 |

**인사이트**: 커리어데이는 단순 키워드 매칭보다 포트폴리오 기반 검증과 프로젝트 적합도 평가에 강점이 있으므로, 스킬 벡터 유사도 + 포트폴리오 품질 지표를 핵심 피처로 설정해야 합니다.

---

## 3. 데이터 수집 방법

### 3.1 수집 주기 및 방법

| 데이터 유형 | 수집 방법 | 주기 | 보존 기간 |
|-----------|---------|------|---------|
| 사용자 프로필 | 회원가입/수정 이벤트 캡처 | 실시간 | 탈퇴 후 30일 |
| 공고/프로젝트 정보 | API 연동 또는 DB 동기화 | 1시간 | 마감 후 180일 |
| 조회/찜 이벤트 | 클라이언트 이벤트 트래킹 | 실시간 (배치 업로드 5분) | 2년 |
| 지원/채용 결과 | 상태 변경 이벤트 | 실시간 | 5년 |
| 매칭 스코어 로그 | 추천 엔진 응답 시 저장 | 실시간 | 1년 |

### 3.2 데이터 스키마 (TypeScript 인터페이스 수준)

```typescript
// 구직자 프로필
interface UserProfile {
  userId: string;
  skills: string[];
  experienceYears: number;
  jobCategory: string;
  location: string;
  salaryRange: { min: number; max: number };
  portfolioCount: number;
  activityScore: number;
  updatedAt: Date;
}

// 공고/프로젝트
interface JobPosting {
  jobId: string;
  requiredSkills: string[];
  preferredSkills: string[];
  experienceLevel: 'junior' | 'mid' | 'senior';
  jobType: 'fulltime' | 'freelance' | 'project';
  industry: string;
  location: string;
  salaryOffered: { type: string; range: [number, number] };
  createdAt: Date;
  expiresAt: Date;
}

// 행동 이벤트
interface UserEvent {
  eventId: string;
  eventType: 'view' | 'bookmark' | 'apply' | 'hire';
  userId: string;
  jobId: string;
  timestamp: Date;
  metadata?: Record<string, unknown>;
}
```

### 3.3 개인정보 처리 방침 (PIPA 준수)

| 항목 | 처리 방침 |
|------|---------|
| 수집 동의 | 회원가입 시 데이터 활용 동의 (선택적 동의 포함) |
| 비식별화 | 분석 시 user_id → 해시값으로 치환 |
| 접근 제어 | 분석 담당자만 익명화 데이터 접근 |
| 삭제 권리 | 탈퇴 요청 시 30일 이내 개인식별 데이터 삭제 |
| 제3자 제공 | 기업에는 매칭 결과만 제공, 원본 프로필 비공개 |

---

## 4. 데이터 전처리 파이프라인

### 4.1 파이프라인 흐름도

```
원시 데이터 수집
      │
      ▼
┌─────────────────┐
│  1. 결측치 처리  │  - 필수 항목 누락 시 기본값 또는 제외
│                 │  - skills 빈 배열 → "미입력" 태그 처리
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  2. 이상치 제거  │  - 경력 연수 < 0 또는 > 50 → 제외
│                 │  - 급여 이상값 (IQR 기준 ±3σ 초과) → 클리핑
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│  3. 정규화/인코딩         │  - 연봉: Min-Max Normalization
│                         │  - 스킬 태그: 멀티-핫 인코딩 또는 임베딩
│                         │  - 직무 카테고리: 레이블 인코딩
└────────────┬────────────┘
             │
             ▼
┌─────────────────────────┐
│  4. 피처 엔지니어링       │  - 스킬 유사도 벡터 생성
│                         │  - 활동 점수 계산 (조회/찜/지원 가중합)
│                         │  - 직무 임베딩 생성 (Word2Vec 또는 TF-IDF)
└────────────┬────────────┘
             │
             ▼
┌─────────────────────────┐
│  5. 학습 데이터셋 구성    │  - Train/Validation/Test = 70/15/15
│                         │  - 시간 기반 분리 (데이터 누수 방지)
└─────────────────────────┘
```

### 4.2 스킬 태그 정규화 규칙

| 원본 입력 | 정규화 결과 | 처리 방식 |
|---------|-----------|---------|
| `"react"`, `"ReactJS"`, `"React.js"` | `"React"` | 대소문자 통일, alias 매핑 |
| `"node"`, `"NodeJS"`, `"Node.js"` | `"Node.js"` | 표준 명칭 통일 |
| `"ML"`, `"머신러닝"`, `"machine learning"` | `"Machine Learning"` | 다국어 alias 매핑 |
| 오타/유사어 | 가장 유사한 표준 태그 | Levenshtein Distance ≤ 2 |

---

## 5. 핵심 분석 항목

### 5.1 매칭 성공/실패 패턴 분석

**분석 목표**: 매칭 성공(채용 완료)과 실패를 결정하는 주요 변수 파악

**분석 방법**:
1. 채용 완료 vs 미채용 그룹 간 피처 비교 (t-test, Mann-Whitney U test)
2. 결정 트리로 주요 분기 변수 추출
3. 상관관계 히트맵으로 피처 간 관계 파악

**핵심 분석 질문**:
- 스킬 일치도(%)와 채용 성공률의 관계는?
- 경력 연수 불일치 허용 범위는?
- 응답 속도(기업 회신 시간)가 성공률에 미치는 영향은?

### 5.2 직무·스킬 키워드 빈도 분석

| 분석 항목 | 방법 | 활용 목적 |
|---------|------|---------|
| 공고 내 스킬 태그 Top 50 | 빈도 카운트 | 트렌드 파악, 추천 가중치 설정 |
| 구직자 프로필 스킬 분포 | 빈도 + 경력 교차 | 수요-공급 갭 분석 |
| 시간대별 스킬 트렌드 | 월별 빈도 변화율 | 신기술 등장 감지 |

### 5.3 구직자 행동 패턴 분석

**전환 퍼널**:
```
노출 (Impression)
    │  노출 대비 조회율 (CTR)
    ▼
조회 (View)
    │  조회 대비 찜 비율
    ▼
찜 (Bookmark)
    │  찜 대비 지원 전환율
    ▼
지원 (Apply)
    │  지원 대비 면접 전환율
    ▼
채용 (Hire)
```

**분석 지표**:
| 지표 | 계산식 | 목표 기준선 |
|------|-------|-----------|
| CTR (클릭률) | 조회 수 / 노출 수 | > 8% |
| 찜 전환율 | 찜 수 / 조회 수 | > 5% |
| 지원 전환율 | 지원 수 / 조회 수 | > 3% |
| 채용 전환율 | 채용 완료 / 지원 수 | > 15% |

---

## 6. 분석 도구 및 시각화 계획

### 6.1 기술 스택
| 역할 | 도구 |
|------|------|
| 데이터 처리 (탐색적 분석) | Python (Pandas, NumPy) |
| 통계 분석 | SciPy, StatsModels |
| 시각화 (분석용) | Matplotlib, Seaborn |
| 프론트 대시보드 시각화 | Chart.js, Recharts |

### 6.2 분석 대시보드 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│              데이터 분석 대시보드                                │
├──────────────────────────┬─────────────────────────────────────┤
│  직무별 매칭 성공률        │  스킬 수요-공급 히트맵              │
│  ┌───────────────────┐   │  ┌───────────────────────────────┐  │
│  │ Bar Chart         │   │  │        Skill Heatmap          │  │
│  │ [프론트엔드] ████ │   │  │  스킬\경력  0-2  3-5  5+      │  │
│  │ [백엔드    ] ████ │   │  │  React    ███  ██   █         │  │
│  │ [데이터    ] ███  │   │  │  Python   █    ███  ███       │  │
│  │ [디자인    ] ██   │   │  │  AWS      ░    █    ███       │  │
│  └───────────────────┘   │  └───────────────────────────────┘  │
├──────────────────────────┼─────────────────────────────────────┤
│  지원 전환 퍼널           │  월별 스킬 트렌드 (Top 5)           │
│  ┌───────────────────┐   │  ┌───────────────────────────────┐  │
│  │ 노출  [████████]  │   │  │ Line Chart                    │  │
│  │ 조회  [██████ ]   │   │  │ ─── React  ── TypeScript      │  │
│  │ 찜    [████   ]   │   │  │ ─── Python ── Kubernetes      │  │
│  │ 지원  [██     ]   │   │  │                               │  │
│  │ 채용  [█      ]   │   │  │ Jan Feb Mar Apr May Jun       │  │
│  └───────────────────┘   │  └───────────────────────────────┘  │
└──────────────────────────┴─────────────────────────────────────┘
```

---

## 7. MVP 산출물 (Deliverables)

> 4일 일정 기준 — Must Have만 표기. Nice to Have는 Day 4 여유 시간에 추가.

| 산출물 | 형태 | 목표 완성 시점 | 우선순위 |
|-------|------|-------------|---------|
| 데이터 스키마 정의 | `types/data.ts` (UserProfile, JobPosting, UserEvent) | Day 1 오전 | Must |
| 목 데이터 세트 | `lib/data/mockData.ts` (공고 30건, 사용자 5명, 이벤트 50건) | Day 1 오후 | Must |
| 스킬 태그 정규화 매핑 | `lib/data/skillMap.json` (주요 50개 태그) | Day 1 오후 | Must |
| KPI 카드 컴포넌트 | `components/dashboard/KPICard.tsx` | Day 3 오전 | Must |
| 기본 분석 대시보드 | CTR·지원전환율·매칭성공률 3개 카드 표시 | Day 3 오후 | Must |
| 전환 퍼널 차트 | `components/dashboard/FunnelChart.tsx` | Day 4 여유 시 | Nice |
| 데이터 분석 결과 보고서 | `docs/ab-results.md` 간략 정리 | Day 4 | Nice |
